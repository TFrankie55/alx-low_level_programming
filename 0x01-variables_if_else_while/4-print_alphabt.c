#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints the letters of the alphabet except q, e
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i = 97;

	while (i < 123)
	{
		if i(i !=101 && i !=113)
		{
			putchar (i);
		}
		i++;
	}
	putchar(10);

	return (0);
}
