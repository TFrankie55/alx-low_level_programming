#include "main.h"

/**
 * _isalpha - Checks for alphabetic characters
 * @c: contains value to be compared
 *
 * Return: Always 0 (Success)
 */
int _isalpha(int c)
{

if ((c > 'a' && c < 'z') || (c > 'A' && c < 'Z'))
{
Return (1);
}

else
{
Return (0);
}

}
