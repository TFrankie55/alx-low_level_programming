#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @i: prints int i
 * @k: prints int k
 * Return: Always 0 (Success)
 */

int add(int i, int k)
{
return (i + k);
}
