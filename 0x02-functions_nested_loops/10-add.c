#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @i: print int i
 * @k: print int k
 * Return: Always 0 (Success)
 */

int add(int, int)
{
return (i + k);
}
